/*
Che cosa è l'ereditarietà ? 

Come mai viene utilizzata ? 
- Permette di creare delle nuove classi da classi esistenti
- La nuova classe contiene i dati e il comportamento della 
classe genitore
- Permette di riutilizzare classi esistenti
- Permette di focalizzarsi sugli attributi comuni in 
un set di classi
- Permette alle nuove classi di modificare il comportamento di classi
esistenti per rendele uniche
    - senza modificare la classe originale

*/

